---
applications:
- name: cf-nodejs
  memory: 128M
  instances: 1
  random-route: true
deployment:
  services:
  - service: redis28
    plan: standard
    label: test-redis
  env:
    SECRET_KEY:
      description: "Secret key"
      required: true
    LABEL:
      description: "Application label"
      value: "My app"
      required: false
